extends layouts/application.pug

block content
  .row.justify-content-center
    .col-12.col-md-8.col-lg-6
      .card.shadow-sm
        .card-body.p-5
          .text-center
            h1.fw-bold= t('views.index.welcome')
            p.text-muted.mb-4= t('views.index.description')
            a.btn.btn-primary.px-4(href="https://ru.hexlet.io")= t('views.index.learnMore')
